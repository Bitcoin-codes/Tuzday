# web-demo removed

The web-demo previously hosted a browser-based demo (CodeMirror + Pyodide). It has been removed from this branch to focus the repository on a fully native Flutter app.

If you need the web demo back, it lives in previous branches (feature/web-demo) and can be restored from Git history.
